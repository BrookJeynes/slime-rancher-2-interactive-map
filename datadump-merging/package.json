{
  "name": "datadump-merging",
  "private": true,
  "version": "0.5.0",
  "description": "Merge data dumped from SR2 via a plugin into the appropriate files for the interactive map",
  "scripts": {
    "clean": "rimraf build/",
    "prebuild": "npm run clean",
    "build": "npx tsc",
    "apply": "npm run build && node build/datadump-merging/apply-dumps.mjs",
    "stitch": "npm run build && node build/datadump-merging/stitch.mjs"
  },
  "author": "rodriguezrrp",
  "license": "MIT",
  "devDependencies": {
    "csv-parse": "^5.5.6",
    "es-main": "^1.3.0",
    "rimraf": "^5.0.7",
    "sharp": "^0.33.4"
  },
  "dependencies": {
    "typescript": "^5.5.2"
  }
}
